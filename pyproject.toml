[project]
name = "monitor"
version = "0.1.0"
description = "Terminal-style dashboard for iPad Mini"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "feedparser>=6.0",
    "requests>=2.31",
    "pyyaml>=6.0",
    "markdown>=3.5",
    "yfinance>=1.4.0",
]
